import clsx from 'clsx'
import React from 'react'

const Buttons = (icon: string, children: string) => {
  return (
    <button className={clsx('')}>
      is it visible
    </button>
  )
}

export default Buttons
